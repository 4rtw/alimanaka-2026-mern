# Backend API base URL (no trailing /api — the proxy route handler adds it)
# For local development: http://localhost:8005
# For production: set to your deployment URL
BACKEND_API_URL=http://localhost:8005

# Liturgical year (optional, defaults to current year)
# LITURGICAL_YEAR=2026
